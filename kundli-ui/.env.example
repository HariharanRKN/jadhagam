# Optional: self-hosted Photon base URL (no trailing slash). Default: https://photon.komoot.io
# See https://github.com/komoot/photon
PHOTON_API_URL=

# Optional: historical planetary SQLite DB bundled with the app image
HISTORY_DB_PATH=
